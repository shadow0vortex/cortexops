apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-topology
spec:
  selector:
    app: cortexops
    service: topology
  ports:
    - port: 9091
      targetPort: 9091
      name: diag
