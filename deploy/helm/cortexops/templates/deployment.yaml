{{- $services := list "collector" "correlator" "topology" "rca" "remediation" -}}
{{- range $service := $services }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $.Release.Name }}-{{ $service }}
  labels:
    app: cortexops
    service: {{ $service }}
spec:
  {{- $svcValues := index $.Values $service }}
  replicas: {{ $svcValues.replicaCount | default $.Values.replicaCount }}
  selector:
    matchLabels:
      app: cortexops
      service: {{ $service }}
  template:
    metadata:
      labels:
        app: cortexops
        service: {{ $service }}
    spec:
      serviceAccountName: {{ $.Release.Name }}-{{ $service }}-sa
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: cortexops
                    service: {{ $service }}
                topologyKey: kubernetes.io/hostname
      containers:
        - name: {{ $service }}
          image: "{{ $.Values.image.repository }}/{{ $service }}:{{ $.Values.image.tag }}"
          imagePullPolicy: {{ $.Values.image.pullPolicy }}
          {{- if $.Values.securityContext }}
          securityContext:
            {{- toYaml $.Values.securityContext | nindent 12 }}
          {{- end }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          env:
            - name: NATS_URL
              value: {{ $.Values.nats.url | quote }}
            - name: TEMPORAL_URL
              value: {{ $.Values.temporal.host | quote }}
            - name: QDRANT_URL
              value: {{ $.Values.qdrant.url | quote }}
            - name: TOPOLOGY_URL
              value: "http://{{ $.Release.Name }}-topology:9091"
            - name: DIAG_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ $.Release.Name }}-diag-token
                  key: token
                  optional: true
          resources:
            {{- toYaml $svcValues.resources | nindent 12 }}
          ports:
            - containerPort: 9091
              name: diag
          livenessProbe:
            httpGet:
              path: /debug/healthz
              port: 9091
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /debug/healthz
              port: 9091
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
      volumes:
        - name: tmp
          emptyDir: {}
{{- end }}
