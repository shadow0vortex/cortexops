apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # Use the docker-compose service name
    url: http://prometheus:9090
    isDefault: true
    editable: false
